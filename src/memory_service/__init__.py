"""memory-service: a memory backend for AI agents.

Ingests conversation turns, extracts structured knowledge with an LLM,
and answers recall queries with hybrid retrieval under a token budget.
"""

__version__ = "0.5.0"
